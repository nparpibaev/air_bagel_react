const styles = (theme) => ({
    main: {
        marginBottom: theme.spacing(5),
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
    },
    input: {
        maxWidth: "5em",
        marginLeft: "1em",
        marginRight: "1em"
    }
});

export default styles;